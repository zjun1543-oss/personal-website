[build]
  command = "npm run build"
  publish = ".next"

[functions]
  node_bundler = "esbuild"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[dev]
  command = "npm run dev"
  port = 3000
  publish = ".next"

# 环境变量需要在 Netlify Dashboard 中设置
# ZHIPU_API_KEY = your_api_key_here
